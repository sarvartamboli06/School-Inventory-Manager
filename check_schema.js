import { createClient } from '@supabase/supabase-js';
import fs from 'fs';

// Read config from js/credentials or something?
// Actually we can just read the js/supabase.js file to see the URL and key
